# 核心依赖
pandas          # 数据处理核心库（需指定版本确保兼容性）[3,5](@ref)
yfinance        # 雅虎财经数据接口（与pandas 2.1.2兼容）[2,7](@ref)
tulip-python

# 隐式依赖（yfinance/pandas的依赖树，需显式声明避免冲突）
numpy           # pandas的底层计算依赖[3,8](@ref)
requests         # yfinance的网络请求库（≥2.31.0解决SSL问题）[7,8](@ref)
lxml              # HTML解析库（yfinance必需）[7](@ref)
multitasking     # yfinance的异步支持库[7](@ref)
